import { sample } from "../services/temp";

describe("Given", () => {
  describe("When", () => {
    it("then", () => {
      let result = sample(1, 1);
      expect(result).toBe(2);
    });
  });

  describe("When", () => {
    it("then", () => {
      let result = sample(1, 1);
      expect(result).toBe(2);
    });
  });
});
